(()=>{'use strict';const input=document.getElementById('requestInput'),form=document.getElementById('requestForm'),count=document.getElementById('charCount'),result=document.getElementById('routeResult'),chat=document.getElementById('chat'),chips=document.querySelectorAll('[data-request]'),chatBtns=document.querySelectorAll('[data-chat]');const routes=[{id:'video',keys:['تیزر','ویدیو','فیلم','کلیپ','محتوا ویدیویی'],title:'مسیر تولید تیزر',desc:'از ایده تا نسخه نهایی و آماده انتشار.',steps:['ایده و هدف','سناریو و متن','طراحی تصویر','تولید و تدوین','صدا و نسخه‌ها','انتشار و رشد']},{id:'website',keys:['سایت','وب سایت','وب‌سایت','وبسایت','فروشگاه','لندینگ','صفحه اینترنتی'],title:'مسیر ساخت سایت',desc:'هدف، ساختار، طراحی، اجرا و رشد را یکجا جلو می‌بریم.',steps:['هدف و مخاطب','ساختار و محتوا','طراحی','توسعه','تست و انتشار','SEO و رشد']},{id:'prototype',keys:['پیش نمایش','پیش‌نمایش','نمونه قبل','قبل از ساخت','prototype','پروتوتایپ','ماکت'],title:'مسیر پیش‌نمایش',desc:'اول ببین، بررسی کن و اصلاح کن؛ بعد وارد اجرای اصلی شو.',steps:['تعریف ایده','نمونه اولیه','پیش‌نمایش','بازخورد','اصلاح','تأیید نهایی']},{id:'fix',keys:['خطا','خراب','درست نمی','مشکل','ارور','کند','کار نمی','رفع'],title:'مسیر رفع مشکل',desc:'مشکل را از تشخیص تا تست نهایی دنبال می‌کنیم.',steps:['دریافت مشکل','تشخیص','اولویت‌بندی','راه‌حل','اجرا','تست و تحویل']},{id:'growth',keys:['فروش','مشتری','رشد','تبلیغ','سئو','seo','بازدید','درآمد'],title:'مسیر رشد',desc:'از هدف کسب‌وکار تا جذب، تبدیل و اندازه‌گیری.',steps:['هدف','مخاطب','پیشنهاد','جذب','تبدیل','اندازه‌گیری']}],fallback={id:'custom',title:'مسیر اختصاصی ANIL X',desc:'اسم خدمت مهم نیست؛ من خواسته‌ات را به قدم‌های قابل اجرا تبدیل می‌کنم.',steps:['فهم خواسته','کشف نیاز','ساخت مسیر','پیش‌نمایش','اجرا','ادامه و رشد']};const normalize=s=>String(s||'').trim().toLowerCase().replace(/[يى]/g,'ی').replace(/ك/g,'ک').replace(/[\u200c\u200d]/g,' '),findRoute=t=>{const x=normalize(t);return routes.find(r=>r.keys.some(k=>x.includes(normalize(k))))||fallback},faNum=n=>String(n).replace(/\d/g,d=>'۰۱۲۳۴۵۶۷۸۹'[d]);function updateCount(){if(count)count.textContent=`${faNum(input.value.length)} / ۱۰۰۰`}function addMsg(who,text,extra=''){const d=document.createElement('div');d.className=`msg ${who}`;d.innerHTML=`<b>${who==='bot'?'بدرخان':'شما'}</b><p></p>${extra}`;d.querySelector('p').textContent=text;chat.appendChild(d);chat.scrollTop=chat.scrollHeight}function renderRoute(text,apiRoute=null,confidence=null){const route=apiRoute||findRoute(text);result.innerHTML='';const h=document.createElement('h3');h.textContent=route.title;const p=document.createElement('p');p.textContent=route.desc;const ul=document.createElement('ol');ul.className='route-list';route.steps.forEach(s=>{const li=document.createElement('li');li.textContent=s;ul.appendChild(li)});const badge=document.createElement('div');badge.className='route-badge';badge.textContent=confidence?`اطمینان تحلیل: ${Math.round(confidence*100)}٪`:'مسیر پیشنهادی بدرخان';result.append(h,p,badge,ul);result.hidden=false;result.scrollIntoView({behavior:'smooth',block:'nearest'})}function botAnswer(text,route){const t=normalize(text);if(/سلام|درود|الو/.test(t))return'سلام! من بدرخانم. هدفت رو بگو؛ از بین مسیرها خودم بهترین راه رو پیدا می‌کنم.';if(route.id==='growth')return'برای رشد، اول هدف و مشتریت را مشخص می‌کنیم؛ بعد پیشنهاد، جذب، تبدیل و اندازه‌گیری را می‌چینیم. لازم نیست خودت دنبال ابزار بگردی.';if(route.id==='website')return'باشه. برای سایت، اول هدف و مخاطب را مشخص می‌کنیم و بعد طراحی و اجرا را جلو می‌بریم. اگر سایت فروشگاهی می‌خواهی، همان را هم در مسیر لحاظ می‌کنم.';if(route.id==='fix')return'مشکل را از خودت نمی‌خوام حدس بزنی. جزئیاتش را همین‌جا بگو یا عکس بفرست تا مسیر تشخیص، اصلاح و تست را مشخص کنم.';if(route.id==='video')return'تیزر را از ایده تا تدوین و نسخه آماده انتشار می‌بریم. اگر محصول یا مخاطب مشخصی داری، همین‌جا اضافه کن.';if(route.id==='prototype')return'اول نمونه می‌سازیم تا قبل از هزینه اصلی ببینی، اصلاح کنی و بعد تأیید نهایی بدهی.';return'فهمیدم. لازم نیست اسم خدمت را بدانی؛ من خواسته‌ات را باز می‌کنم و قدم بعدی را مشخص می‌کنم. نتیجه‌ای که می‌خواهی را هم بگو.'}async function handle(text){if(!text)return;addMsg('user',text);const typing=document.createElement('div');typing.className='msg bot typing';typing.innerHTML='<b>بدرخان</b><p>دارم بررسی می‌کنم…</p>';chat.appendChild(typing);chat.scrollTop=chat.scrollHeight;try{const r=await fetch('/api/analyze',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({text})});const data=await r.json();typing.remove();const route=data.ok?data.route:findRoute(text);addMsg('bot',botAnswer(text,route));renderRoute(text,route,data.confidence||null)}catch{typing.remove();const route=findRoute(text);addMsg('bot',botAnswer(text,route));renderRoute(text,route)}}chips.forEach(c=>c.addEventListener('click',()=>{input.value=c.dataset.request;updateCount();handle(input.value)}));chatBtns.forEach(c=>c.addEventListener('click',()=>{input.value=c.dataset.chat;updateCount();handle(input.value)}));input.addEventListener('input',updateCount);form.addEventListener('submit',e=>{e.preventDefault();const text=input.value.trim();if(!text){input.focus();return}input.value='';updateCount();handle(text)});const voice=document.getElementById('voiceBtn'),status=document.getElementById('voiceStatus');if(voice&&('webkitSpeechRecognition'in window||'SpeechRecognition'in window)){const SR=window.SpeechRecognition||window.webkitSpeechRecognition;const rec=new SR();rec.lang='fa-IR';rec.interimResults=false;voice.addEventListener('click',()=>{if(rec._active)return;rec._active=true;voice.setAttribute('aria-pressed','true');if(status){status.hidden=false;status.textContent='دارم گوش می‌دم…'}try{rec.start()}catch{rec._active=false;voice.setAttribute('aria-pressed','false')}});rec.onresult=e=>{const text=e.results[0][0].transcript;input.value=text;updateCount();if(status)status.textContent='گرفتم؛ حالا بدرخان بررسی می‌کند.';handle(text);input.value='';updateCount()};rec.onerror=()=>{if(status){status.hidden=false;status.textContent='ورودی صوتی روی این مرورگر در دسترس نیست.'}rec._active=false;voice.setAttribute('aria-pressed','false')};rec.onend=()=>{rec._active=false;voice.setAttribute('aria-pressed','false')}}else if(voice){voice.addEventListener('click',()=>{if(status){status.hidden=false;status.textContent='برای بگو، مرورگر را با پشتیبانی از ورود صوتی باز کن.'}})}const image=document.getElementById('imageInput'),preview=document.getElementById('imagePreview');if(image&&preview)image.addEventListener('change',()=>{const f=image.files&&image.files[0];if(!f)return;preview.hidden=false;preview.textContent=`فایل «${f.name}» آماده است؛ حالا توضیحت را بنویس و «بدرخان، بررسی کن» را بزن.`;input.focus()});updateCount()})();