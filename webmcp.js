(()=>{'use strict';\nconst ready=()=>Boolean(document.modelContext&&typeof document.modelContext.registerTool==='function');\nconst safe=(fn,fb)=>{try{return fn()}catch{return fb}};\nconst getDNA=()=>safe(()=>{const d=JSON.parse(localStorage.getItem('anilx_experience_dna_v1')||'{}');return{mode:d?.prefs?.mode||'adaptive',format:d?.prefs?.format||'text',goal:d?.prefs?.goal||'discover',discovery:Boolean(d?.prefs?.discovery)}},{mode:'adaptive',format:'text',goal:'discover',discovery:false});\nconst route=()=>({goal:(document.querySelector('#requestInput')||document.querySelector('#q'))?.value?.trim()||'',mode:document.documentElement.dataset.axMode||'adaptive'});\nasync function register(){\n if(!ready())return false;\n const tools=[\n {name:'anilx_get_experience',description:'Read the user-controlled coarse ANIL X experience preferences. Never expose raw interaction history or private data.',inputSchema:{type:'object',properties:{}},annotations:{readOnlyHint:true,untrustedContentHint:false,consequentialHint:false},execute:async()=>JSON.stringify(getDNA())},\n {name:'anilx_get_current_request',description:'Read the current visible ANIL X request and adaptive mode so an agent can understand the current starting state.',inputSchema:{type:'object',properties:{}},annotations:{readOnlyHint:true,untrustedContentHint:false,consequentialHint:false},execute:async()=>JSON.stringify(route())},\n {name:'anilx_set_request',description:'Place a user-approved goal into the ANIL X request composer. This does not submit, purchase, publish, or modify external data.',inputSchema:{type:'object',properties:{request:{type:'string',description:'The goal or request the user explicitly wants to work on.'}},required:['request']},annotations:{readOnlyHint:false,untrustedContentHint:false,consequentialHint:false},execute:async({request})=>{const input=document.querySelector('#requestInput');if(!input)return'ANIL X request composer is unavailable.';input.value=String(request||'').slice(0,4000);input.dispatchEvent(new Event('input',{bubbles:true}));input.focus();return'Request placed in the composer; it has not been submitted or executed externally.'}},\n {name:'anilx_start_discovery',description:'Open the ANIL X discovery layer without spending money, changing account data, or performing an external side effect.',inputSchema:{type:'object',properties:{}},annotations:{readOnlyHint:false,untrustedContentHint:false,consequentialHint:false},execute:async()=>{document.body.classList.add('ax-discovery-mode');const el=document.querySelector('#axInspect');if(el)el.click();return'ANIL X discovery layer activated.'}}\n ];\n for(const tool of tools){try{await document.modelContext.registerTool(tool)}catch(e){}}\n window.ANILX_WEBMCP={ready:true,tools:tools.map(t=>t.name),registered:true,version:'AX-WEBMCP-1'};\n return true;\n}\nconst boot=()=>register().catch(()=>false);\nif(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot();\n})();